class Wrong{
   constructor(){}
    authenticate(accessCode,enteredCode){
        textSize(50);
        fill("black");
        text(code,300,300)
    if(accessCode !== enteredCode()){
        return false
        gameState = end; 
    }
}
}
